<% provide(titlle: @user.name) %>
<div class="row">
    <div class="medium-4 columns">
        <h1> 
            <%= gravatar_for @user.name %>
            <%= @user.name %>
        </h1>
    </div>
</div>
